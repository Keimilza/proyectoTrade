{% extends "base.html.twig" %}

{% block title %}TradeBundle:Usuario{% endblock %}

{% block body %}
<h1>Registro Usuario</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
{{ form_end(form) }}
<a class="btn btn-outline-secondary" href="{{ path('login') }}" role="button">Login</a>
{% endblock %}